# Keep the build context (and image) small and secret-free.
.git
.gitignore
.venv
__pycache__
*.py[cod]
.pytest_cache
.ruff_cache

# Secrets — never ship .env into the image.
.env

# Not needed at runtime.
tests
synthetic-logs
*.md
Dockerfile
.dockerignore
fly.toml
.python-version
